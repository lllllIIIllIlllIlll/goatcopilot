package mods;

// HUD with coordinates, compass, direction, etc.
public class ModDirectionHUD extends Mod {
    public ModDirectionHUD() { super("Direction HUD"); }
    @Override
    public void onRender2D(float partialTicks) {
        // TODO: Draw coordinates, direction, compass, etc.
    }
}